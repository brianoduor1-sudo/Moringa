1. Create your project folder
2. Create your virtual environment

3. Install pipenv (if not already installed)
   pip install pipenv

4. Create the virtual environment with Python 3.8
   pipenv --python 3.8

5. Install Flask into the environment
   pipenv install flask

6. Activate the virtual environment
   pipenv shell

7. Run the app
   python app.py
